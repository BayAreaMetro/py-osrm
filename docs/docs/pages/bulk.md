# Bulk Processing

Process large origin-destination matrices in parallel. Works with both the native `OSRM` engine
and the `OSRM_HTTP` client.

## bulk_route

::: osrm.bulk_route
    options:
      show_root_heading: true

---

## bulk_nearest

::: osrm.bulk_nearest
    options:
      show_root_heading: true

---

## bulk_match

::: osrm.bulk_match
    options:
      show_root_heading: true
